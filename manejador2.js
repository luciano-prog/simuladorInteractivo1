let consulta = prompt(
  "quiere ingresar a nuestro torneo de futbol?"
).toUpperCase();

alert(" Disfruta del futbol y recomendanos a tus amigos⚽");

/*while (consulta.toUpperCase() != "ESC") 
  

  switch (consulta.toUpperCase()) {
    case "SI":
        let nombre = prompt ("ingrese el nombre de su equipo a inscribir");
        document.write("🎉Felicitaciones!"+ nombre + "  será parte del torneo😁 los estaremos contactando");
        console.log(nombre + " pertenece al torneo👍");
        break;
    case "NO": 
      let consulta2 = prompt("quieres jugar individualmente para un equipo").toUpperCase()
      if (consulta2.toUpperCase() = "SI") {
        console.log( "Contactanos al teléfono detallado más abajo")
        document.write("Contactanos a la brevedad 📧");}
      else {
        document.write(" Disfruta del futbol y recomendanos a tus amigos⚽");
      } 
      break;   
    default:
        document.write(" Disfruta del futbol y recomendanos a tus amigos⚽");  
      break;
  }*/
